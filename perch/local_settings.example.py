"""
Local, untracked overrides for sensitive settings.

Copy this file to `local_settings.py` (same directory) and fill in values.
Never commit `local_settings.py` to version control.
"""

# Example Google OAuth credentials (replace with real values on each machine)
GOOGLE_OAUTH_CLIENT_ID = "your-google-client-id.apps.googleusercontent.com"
GOOGLE_OAUTH_CLIENT_SECRET = "your-google-client-secret"

# Optionally override the admin email used for in-app admin role
# PERCH_ADMIN_EMAIL = "admin@bc.edu"

